const menu=document.querySelector('.menu');const nav=document.querySelector('.nav-links');if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))})}
const filters=[...document.querySelectorAll('.filter')];const searchable=[...document.querySelectorAll('[data-category]')];const search=document.querySelector('[data-search]');function update(){const active=document.querySelector('.filter.active')?.dataset.filter||'all';const q=(search?.value||'').trim().toLowerCase();let shown=0;searchable.forEach(item=>{const category=item.dataset.category;const text=item.textContent.toLowerCase();const visible=(active==='all'||category===active)&&(!q||text.includes(q));item.style.display=visible?'':'none';if(visible)shown++});const empty=document.querySelector('.empty');if(empty)empty.style.display=shown?'none':'block'}filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));button.classList.add('active');update()}));if(search)search.addEventListener('input',update);

